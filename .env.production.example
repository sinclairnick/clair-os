# ClairOS Production Environment
# Copy this file and fill in production values

# Database (generate strong password)
DB_PASSWORD=<generate-strong-password>

# BetterAuth Secret (generate with: openssl rand -base64 32)
BETTER_AUTH_SECRET=<generate-with-openssl-rand-base64-32>

# BetterAuth Base URL (your production domain)
BETTER_AUTH_URL=https://home.sinclairs.app

# Google OAuth (reuse from dev .env)
GOOGLE_OAUTH_CLIENT_ID=<from-your-env>
GOOGLE_OAUTH_CLIENT_SECRET=<from-your-env>

# GHCR Image Registry

IMAGE_REGISTRY=ghcr.io
IMAGE_REPO=sinclairnick/clair-os

# Minio (Storage) - generate strong password
MINIO_ROOT_USER=clairos_admin
MINIO_ROOT_PASSWORD=<generate-strong-password>
MINIO_PUBLIC_URL=https://static.home.sinclairs.app

# Push Notifications (generate with: cd api && bun run vapid:generate)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@sinclairs.app
